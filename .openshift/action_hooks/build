cd ${OPENSHIFT_REPO_DIR}
pip install -r requirements.txt --no-cache-dir

mv ${OPENSHIFT_DATA_DIR}/config ${OPENSHIFT_DATA_DIR}/config.bak
cp ${OPENSHIFT_REPO_DIR}/config ${OPENSHIFT_DATA_DIR}/config
echo 'config file copied.'